
enum OnlineStatus {
  ONLINE
  OFFLINE
}

type OnlineStatusEvent {
  id: ID!
  status: OnlineStatus
}

type Subscription {
  # Is triggered when a user's online / offline status changes
  userOnlineStatusChanged: OnlineStatusEvent
}
