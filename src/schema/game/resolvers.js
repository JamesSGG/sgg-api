
export default {
  Game: {
    id(obj) {
      return obj.id
    },

    gameTitle(obj) {
      return obj.game_title
    },
  },

  GamePlatform: {
    id(obj) {
      return obj.id
    },

    platformName(obj) {
      return obj.platform_name
    },
  },
}
